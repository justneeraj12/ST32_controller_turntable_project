{
  "version": "0.2.0",
  "configurations": [
    
    {
      "type": "stlinkgdbtarget",
      "request": "launch",
      "name": "STM32: ST-Link Launch (Manual ELF)",
      "deviceName": "STM32G0B1RE",
      "deviceCore": "Cortex-M0+",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "STM32: Verify Launch (auto-fix)",
      "runEntry": "main",
      "imagesAndSymbols": [
        {
          "imageFileName": "${workspaceFolder}/firmware/Debug/firmware.elf"
        }
      ]
    }
  ]
}
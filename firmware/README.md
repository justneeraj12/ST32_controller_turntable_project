# Firmware

Recommended layout:

- `Core/Inc` and `Core/Src` for CubeMX-generated project code
- `Drivers/` for HAL and BSP dependencies
- `App/` for custom application logic (PID, diagnostics, communication)
